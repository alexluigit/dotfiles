#!/bin/sh
for pid in $(pgrep emacs); do kill -9 $pid; done
[[ -n $1 ]] && rm ~/.cache/emacs/ale.el* 2>/dev/null
[[ -n $2 ]] && rm -rf ~/.cache/emacs/{straight,eln} 2>/dev/null
emacs --daemon
xdotool set_desktop 1
emacsclient -c --no-wait
