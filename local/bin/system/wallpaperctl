#!/usr/bin/env sh
DEFAULT="$HOME/Pictures/wallpaper/awesome.png"

_draw() { xwallpaper --output DP-0 --zoom $1; }
_shuf() { fd -tf . ~/Pictures/wallpaper | shuf -n 1; }

boot() { _draw $DEFAULT; }
change() {
  [[ -n $1 ]] && _draw $1 || _draw $(_shuf)
}

"$@"
